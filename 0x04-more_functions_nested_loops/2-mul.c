#include "main"

/**
 * mul - Multiplies two integers
 * @g: The first number to multiply
 * @h: The second number to be multiplied with the first number
 * Return: The result of multiplication between g param  and h param
 */

int mul(int g, int h);
{
	return (g * h);
}
